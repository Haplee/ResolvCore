# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Herramientas AI (no forman parte del proyecto)
.agents/
claude/
gemini/
opencode/

# Variables de entorno sensibles
.env
.env.local
.env.*.local

# Cuentas de prueba (credenciales locales)
notes/test_accounts.md

# Python
__pycache__/
*.pyc
*.pyo

# Diagnósticos generados (datos locales con hostnames/IPs reales)
scripts/diagnosticos/

# Empaquetados del tema/plugin (distribuir vía GitHub Releases)
wordpress/*.zip
builds/*.zip

# Vendor MantisBT (instalar con: bash scripts/server/bootstrap-mantis.sh)
mantisbt-2.28.1/

# Análisis de grafos generados (herramienta externa, no fuente del proyecto)
graphify-out/

# Agent rules de herramientas AI dentro de carpetas de scripts
scripts/**/.agent/
.deploy-creds.local.txt

# WordPress local dev (LocalWP). wp-config con credenciales locales y dumps de BD.
# Cualquier dump completo de WP contiene wp_users (hashes), emails y posibles
# tokens guardados en wp_options — fuera del repo siempre.
wp-config.php
wordpress-db.sql
wp-content.tar.gz

# Instalación WordPress completa (core + wp-config con RC_MANTIS_TOKEN + vendor).
# El código fuente del proyecto vive en wordpress/ — NUNCA en wp/.
# Auditoría A1/A2 (2026-05-29): wp/ traía 3119 ficheros (95% del repo) y el token
# Mantis filtrado en wp/wp-config.php. Instalar WP vía deploy, no versionarlo.
wp/

# Config server PHP — no pertenece al repo de aplicación (documentar valores en docs/).
/php.ini

# Dumps/concatenados generados de la documentación (los originales viven en docs/).
docs_dump.txt
ResolvCore_Documentacion_Unificada.md
docs/build/

# Snapshot completo del repo para LLMs (~40 MB). Local-only, jamás versionar.
repomix-output.xml
